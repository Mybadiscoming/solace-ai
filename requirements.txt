# overwrite requirements.txt with versions compatible with Python 3.10
cat > requirements.txt <<'REQ'
fastapi==0.95.2
uvicorn[standard]==0.23.0
pydantic==1.10.13
numpy==1.26.4
scikit-learn==1.8.0
jinja2==3.1.6
diskcache==5.4.0
gunicorn==20.1.0
REQ

# show the file so you can confirm quickly
sed -n '1,200p' requirements.txt

# stage + commit + push
git add requirements.txt
git commit -m "Pin prod deps to Python 3.10-compatible versions"
git push origin main